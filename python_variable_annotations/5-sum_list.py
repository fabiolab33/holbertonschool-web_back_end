#!/usr/bin/env python3
"""
Module that contains a function to sum a list of floats.
"""
from typing import List


def sum_list(input_list: List[float]) -> float:
    """
    Sums all elements in a list of floats and returns the result.
    """
    return sum(input_list)
